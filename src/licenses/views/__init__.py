"""View: first-party HTML and the machine JSON API.

- `customer` — customer pages
- `api` — JSON / OpenAPI
- `forms` / `schemas` — HTML fields and JSON schemas; the JSON error envelope is built in `LicenseAPI`

The Admin console stays in `licenses/admin.py` for Django autodiscover.
"""
