# Copy to .kamal/secrets, chmod 600, then fill both values. Never commit real secrets.
# GHCR token with package read/write access for this image namespace.
KAMAL_REGISTRY_PASSWORD=
# Generate ONCE: python3 -c 'import secrets; print(secrets.token_urlsafe(48))'
LICENSE_SESSION_SECRET=
